#ifndef CALLMEMAYBE_STATIC_QUERY_HPP
#define CALLMEMAYBE_STATIC_QUERY_HPP

#include "cmm/static_meta.hpp"

// Compatibility header: the static query facade now lives in static_meta.hpp.
// Keep this include path so existing users can include cmm/static_query.hpp
// without introducing duplicate inline definitions.

#endif // CALLMEMAYBE_STATIC_QUERY_HPP
